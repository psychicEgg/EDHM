Click on the shader file (it will have a .txt extension)

You will see the shader code appear

To download that file, right click on the 'Raw' button (top-right, above the code)

Then select 'Save Link As' (in Chrome, or the equivalent in your browser)

And save the file to your PC
